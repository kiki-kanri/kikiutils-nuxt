import { ElMessage }  from 'element-plus';

export const errorMessage = ElMessage.error;
export const infoMessage = ElMessage.info;
export const successMessage = ElMessage.success;
export const warningMessage = ElMessage.warning;
